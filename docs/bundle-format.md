# Bundle Format

A bundle is a resolved profile plus its source manifests, checksums, state rows, raw artifacts, normalized derivatives, chunks, indexes, services metadata, and attribution records under one relocatable library root.
